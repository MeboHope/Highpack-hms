import { Building2, ChevronDown, MapPin } from 'lucide-react';
export type AssetSwitcherItem = { id:string; name:string; subtitle?:string; meta?:string };
export function AssetSwitcher({items,value,onChange,label='Active asset'}:{items:AssetSwitcherItem[];value?:string;onChange:(id:string)=>void;label?:string}){
 if(!items.length)return null;
 return <div className="rounded-2xl border border-ink-100 bg-white/90 p-3 shadow-sm backdrop-blur"><div className="flex flex-col gap-3 sm:flex-row sm:items-center"><div className="flex min-w-0 items-center gap-3 px-1"><div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-brand-50 text-brand-700"><Building2 className="h-5 w-5"/></div><div className="min-w-0"><p className="text-[10px] font-bold uppercase tracking-[0.16em] text-brand-700">{label}</p><p className="truncate text-sm font-semibold text-ink-900">{items.find(i=>i.id===value)?.name||items[0].name}</p></div></div><div className="relative min-w-0 flex-1"><select aria-label={label} value={value||items[0].id} onChange={e=>onChange(e.target.value)} className="input w-full appearance-none pr-10">{items.map(i=><option key={i.id} value={i.id}>{i.name}{i.subtitle?` · ${i.subtitle}`:''}{i.meta?` · ${i.meta}`:''}</option>)}</select><ChevronDown className="pointer-events-none absolute right-3 top-1/2 h-4 w-4 -translate-y-1/2 text-ink-400"/></div></div>{items.length>1&&<p className="mt-2 flex items-center gap-1 px-1 text-[11px] text-ink-400"><MapPin className="h-3 w-3"/> Your workspace is scoped to the selected asset.</p>}</div>
}
